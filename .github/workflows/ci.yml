name: CI Test Runner

on:
  push:
    branches: [ main, master, homework2 ]
  pull_request:
    branches: [ main, master, homework2 ]

jobs:
  test-server:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./homework/howework_2/real_time_coding_platform/server
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: homework/howework_2/real_time_coding_platform/server/package-lock.json
          
      - name: Install Dependencies
        run: npm ci
        
      - name: Run Server Tests
        run: npm test

  test-client:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./homework/howework_2/real_time_coding_platform/client
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: homework/howework_2/real_time_coding_platform/client/package-lock.json
          
      - name: Install Dependencies
        run: npm ci
        
      - name: Run Client Tests
        run: npm test
